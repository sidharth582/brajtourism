// API server removed per project request.
// Previously this file provided a simple Express endpoint to collect enquiries.
// Keeping this placeholder avoids broken npm scripts that reference the file.
console.log("server/api.js placeholder — API removed");
